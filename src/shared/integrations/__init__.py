"""Integration management for Mantissa Log alert routing.

Submodules are imported on-demand rather than eagerly to avoid coupling
unrelated subpackages. Import the class you need directly, e.g.

    from shared.integrations.integration_manager import IntegrationManager
    from shared.integrations.slack.app import SlackApp
"""
